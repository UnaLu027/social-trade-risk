[build]
builder = "NIXPACKS"

[deploy]
preDeployCommand = "alembic upgrade head && python scripts/seed_data.py"
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
