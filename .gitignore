# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.env
venv/
env/
.venv/

# Backend ML artifacts (committed intentionally)
# backend/app/ml/models/*.pkl  <- DO commit these
# Experiment logs (local only — too many to track, use metadata files instead)
backend/app/ml/experiments/*.json

# Database files (local dev only)
*.db
*.sqlite

# Node
node_modules/
frontend/dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Test artifacts
.pytest_cache/
htmlcov/
.coverage
backend/test.db

php-api/config/config.local.php

# Transient monitoring intermediate file (used only within a workflow run)
generated-data/monitoring-previous.json

# Local virtual environments (non-standard names)
backend/.venv312/
tools/

# Annotation & training data work files (human-label pipeline)
# annotation_queue_template.csv is intentionally committed — exclude it from the glob
backend/app/ml/training/data/annotation_queue_*.csv
!backend/app/ml/training/data/annotation_queue_template.csv
backend/app/ml/training/data/*_labeled.csv
backend/app/ml/training/data/human_labels_v*.csv
backend/app/ml/training/data/training_social_posts_v*.csv
